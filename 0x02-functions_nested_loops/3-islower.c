#include "main.h"
#include <stdlib.h>
#include <unistd.h>
#include <stdio.h>
#include <ctype.h>
/**
 * _islower - checks for lowercase characters
 * Return: 1 if successful, otherwise 0
 */
int _islower(int c)
{
	if (islower(int c));
		_putchar(c);
		return (1);

	else
		_putchar('\n');
		return (0);
}
