#include <stdio.h>
#include <stdlib.h>

/**
 * main - prints number of arguments
 * @argc: argument count
 * @argv: array of arguments
 * Return: 0 value
 */

int main(int argc, char *argv[])
{
	argv = argv;
	printf("%d\n", argc - 1);
	return (0);
}
