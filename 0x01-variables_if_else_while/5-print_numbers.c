#include <stdlib.h>
#include <stdio.h>

/**
 *  main - prints all single digits, base 10
 *  Return: 0 value always
 */
int main(void)
{
	int d;

	for (d = 0; d <= 10; d++)
	printf("%d\n", d);
	return (0);
}
